apiVersion: v1
kind: Service

metadata:
  name: sre-go-web-app

spec:
  selector:
    app: sre-go-web-app

  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 8080

  type: LoadBalancer